# Grafana Dashboard Provisioning Configuration

apiVersion: 1

providers:
  - name: 'RuleIQ Dashboards'
    orgId: 1
    folder: 'RuleIQ'
    folderUid: 'ruleiq'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards