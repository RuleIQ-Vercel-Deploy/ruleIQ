# Dependency constraints to ensure compatible versions across all environments
# This file is used with pip install -c constraints.txt to enforce version compatibility

# Enforce typing-extensions version to resolve conflicts between pydantic, fastapi, and langchain
typing-extensions>=4.11,<5

# Ensure compatible httpx version (prevent duplicate entries)
httpx==0.27.0

# Core dependencies that should remain consistent
pydantic==2.7.4
fastapi==0.110.0
uvicorn==0.27.0